// Settings strings. Every knob gets a `Hint` — a sentence of plain language
// saying what it does and what breaks if it is wrong. Most are lifted straight
// from the Go field comments, which already carry the real numbers measured on
// real machines; a hint that says "controls the beacon threshold" tells the
// reader nothing they could not guess from the label.
export default {
  en: {
    pageDesc: 'Everything the gateway can be configured to do, in one place. Defaults come from the gateway itself.',

    grpGateway: 'Gateway',
    grpGatewayDesc: 'How traffic reaches the gateway and how much of it is captured.',
    grpPolicy: 'Policy',
    grpPolicyDesc: 'What happens to traffic once it is here.',
    grpDetection: 'Detection',
    grpDetectionDesc: 'What counts as suspicious, and what the gateway does about it.',
    grpSystem: 'System',
    grpSystemDesc: 'Files, accounts and this installation.',
    grpUI: 'Interface',

    capture: 'Capture mode',
    captureHint:
      'Manual = clients point at the proxy port themselves. System = the OS proxy settings are set for you. TUN = a virtual interface captures ALL traffic, including a malicious process using a raw socket. TUN needs root and conflicts with other VPN/TUN tools. Changing this arms a 60-second dead-man\'s switch.',
    routing: 'Routing mode',
    routingHint:
      'Rule follows your permit gate and route rules. Global turns default-deny OFF — anything unlisted goes out through the proxy group. The blacklist, threat intel and process/device floor still apply in both.',
    posture: 'Security posture',
    postureHint:
      'Strict = default-deny: only permitted destinations leave at all. Split = default-allow: unmatched traffic follows Final. Each posture keeps its own ACLs, packs and DNS, so switching does not merge them.',
    tunAdvanced: 'TUN options',
    tunAdvancedHint: 'Stack, MTU, strict route, Linux auto-redirect and the interface addresses. Only takes effect in TUN capture mode.',

    inbound: 'Proxy inbound',
    inboundHint:
      'Where the proxy itself listens. Moving it disconnects every client pointed at the old address, so the change is guarded: it reverts unless you confirm from the new one.',
    inboundTitle: 'Proxy inbound listener',
    inboundDesc: 'The mixed socks/http port your clients connect to.',
    inboundListen: 'Listen address',
    inboundListenHint:
      '127.0.0.1 keeps the proxy on this machine. 0.0.0.0 opens it to your network — only do that if an account has a proxy password, otherwise anyone who can reach this machine can use it anonymously.',
    inboundPort: 'Port',
    inboundPortHint: 'Must not collide with the console (21585) or the internal Clash API (21586).',
    inboundGuard: 'Revert in {{sec}}s unless confirmed',
    inboundGuardHint:
      'The gateway puts the old address back if nobody confirms from the new one. This is what keeps a typo from costing you access to a remote machine.',
    inboundExposeWarn:
      'Listening off 127.0.0.1 exposes the proxy to your whole network. Give an account a proxy password first (Accounts page), or the gateway will refuse.',
    inboundSaved: 'Inbound listener applied — reconnect your clients to {{addr}}',
    inboundRevert: 'Inbound listener reverts to {{addr}} in {{sec}}s',
    inboundKeep: 'Keep new address',
    inboundConfirmed: 'New listen address kept',

    final: 'Final egress',
    finalHint:
      'Where permitted traffic goes when no route rule claims it. `proxy` sends it through your nodes; `direct` sends it out of this machine unchanged.',
    dns: 'DNS resolver policy',
    dnsHint: 'Servers, split rules and the direct-route resolver. DNS has to follow routing or mainland sites resolve to your exit node\'s CDN edges.',
    proxyTuning: 'Node failover & scoring',
    proxyTuningHint: 'How eagerly groups switch nodes, and how nodes are scored from real traffic rather than probes.',

    autoBlock: 'Auto-disposal',
    autoBlockHint:
      'Threat-intel hits and exfil-shaped uploads to destinations that are NOT permitted get killed and quarantined. Heuristics (beaconing, DGA) only ever alert — they never cut a connection.',
    warmPermit: 'Wait for the permit index',
    warmPermitHint:
      'Remote rule sets load asynchronously. Until they land, everything they permit reads as "not permitted" — disposing during that window would quarantine destinations you actually approved. Alerts still fire; only disposal waits.',
    detThresholds: 'Connection thresholds',
    detThresholdsHint: 'Beaconing, DGA and exfiltration thresholds. Every one of these was a compiled-in constant until it turned out nobody can tune an alert stream they have to rebuild the binary to change.',
    detObservation: 'Query & host observation',
    detObservationHint: 'DNS query-level detection, client DoH/DoT bypass, route-table watching and JA4 fingerprint learning.',

    retention: 'Logs & history',
    retentionHint:
      'How much disk the daemon log and the connection history are allowed to use. These used to be start-up arguments only, so a plain re-install silently reset them.',
    retentionTitle: 'Log & history retention',
    retentionDesc: 'Rotation for <data>/serve.log and <data>/history.jsonl.',
    retentionLog: 'Daemon log',
    retentionHistory: 'Connection history',
    maxSize: 'Rotate past (MB)',
    maxSizeHint: 'Start a new file when the current one reaches this size. -1 never rotates (log only).',
    maxBackups: 'Generations to keep',
    maxBackupsHint: 'Older generations beyond this count are deleted.',
    maxAge: 'Delete older than (days)',
    maxAgeHint: '0 = keep by count only, never by age.',
    compress: 'gzip rotated files',
    compressHint: 'Compression runs asynchronously, so one generation briefly exists as both .jsonl and .jsonl.gz.',
    historyNoRotateHint: 'History cannot be set to never rotate — an unbounded append-only file eventually fills the disk.',
    retentionSaved: 'Retention applied',

    openReg: 'Open registration',
    openRegHint: 'Lets anyone who can reach the console create an account. Off by default; new accounts are clients, never admins.',
    install: 'Installation',
    version: 'Version',
    dataDir: 'Data directory',
    privileged: 'Privileged',

    language: 'Language',
    languageHint: 'Console display language. Defaults to your system language; a choice here is saved in this browser.',
    theme: 'Theme',
    themeHint: 'Follows your system setting unless you pick one. Saved in this browser.',
    themeSystem: 'System',
    themeDark: 'Dark',
    themeLight: 'Light',

    tun: {
      title: "TUN options",
      desc: "Advanced tuning for TUN capture mode (full-device VPN that captures ALL traffic). Only applies when the capture mode is TUN; ignored in Manual/System.",
      stack: "Network stack",
      stackDesc: "gvisor = userspace, safe & portable · system = faster, needs privileges · mixed.",
      mtu: "MTU",
      mtuPh: "0 = auto",
      strict: "Strict route",
      strictDesc: "Force every packet through the TUN (recommended — prevents leaks).",
      redirect: "Auto redirect (Linux)",
      redirectDesc: "nftables redirect so Docker/containerd bridge containers hit the same Permit/detect path. No-op on macOS/Windows. Needs nft.",
      address: "TUN addresses",
      addressPh: "one CIDR per line — empty = 198.18.0.1/30 (avoids Docker)",
      addressDesc: "Interface CIDRs for the TUN device. Leave empty for the default outside Docker’s 172.16/12 range.",
      redirectNftMissingTitle: "Missing nftables for auto-redirect",
      redirectNftMissingBody: "Linux TUN auto-redirect needs nftables. {cmd} (run as root/admin). If auto-install is not available, install nftables manually.",
      redirectNftInstall: "Install nftables",
      redirectNftInstalled: "nftables installation finished — verify in log/status",
      exclude: "Exclude apps",
      excludePh: "one per line — routed AROUND the TUN",
      excludeDesc: "These apps bypass the TUN and go direct (Android package names).",
      include: "Include only",
      includePh: "one per line — ONLY these go INTO the TUN",
      includeDesc: "If set, ONLY these apps are captured (mutually exclusive with Exclude).",
      save: "Save",
      toast: "TUN options saved",
    },

    restoreDefaults: 'Restore defaults',
    save: 'Save',
    cancel: 'Cancel',
    defaultNote: '(default {{v}})',
    usingDefault: 'default',
  },
  zh: {
    pageDesc: '网关的全部可配置项都在这里。默认值由网关本身给出。',

    grpGateway: '网关',
    grpGatewayDesc: '流量怎么进到网关，以及捕获到什么程度。',
    grpPolicy: '策略',
    grpPolicyDesc: '流量进来之后怎么处理。',
    grpDetection: '检测',
    grpDetectionDesc: '什么算可疑，以及网关拿它怎么办。',
    grpSystem: '系统',
    grpSystemDesc: '文件、账号与这次安装本身。',
    grpUI: '界面',

    capture: '捕获模式',
    captureHint:
      'Manual＝客户端自己指向代理端口；System＝替你设置系统代理；TUN＝虚拟网卡接管**全部**流量，木马的裸 socket 也逃不掉。TUN 需要 root，且与其他 VPN/TUN 工具互斥。切换会武装 60 秒死亡开关。',
    routing: '路由模式',
    routingHint:
      'Rule 遵循你的许可闸与路由规则；Global 把默认拒绝**关掉**——未列入的流量一律经代理组出网。两种模式下黑名单、威胁情报、进程/设备地板都照常生效。',
    posture: '安全姿态',
    postureHint:
      'Strict＝默认拒绝，只有被许可的目的地能出网；Split＝默认放行，未命中的走 Final。两种姿态各自持有独立的 ACL/策略包/DNS，切换不会把它们合并。',
    tunAdvanced: 'TUN 高级选项',
    tunAdvancedHint: '网络栈、MTU、严格路由、Linux auto-redirect 与接口地址。仅在 TUN 捕获模式下生效。',

    inbound: '代理入站',
    inboundHint:
      '代理自己监听在哪。改动会把所有指向旧地址的客户端踢下线，因此带守卫：不从新地址确认就自动回滚。',
    inboundTitle: '代理入站监听点',
    inboundDesc: '客户端连接的 mixed socks/http 端口。',
    inboundListen: '监听地址',
    inboundListenHint:
      '127.0.0.1 把代理留在本机；0.0.0.0 会开放给整个局域网——只有在某个账号设了代理密码时才该这么做，否则任何能连到这台机器的人都能匿名使用它。',
    inboundPort: '端口',
    inboundPortHint: '不能与控制台（21585）或内部 Clash API（21586）冲突。',
    inboundGuard: '{{sec}} 秒内未确认则回滚',
    inboundGuardHint: '没人从新地址确认，网关就把旧地址放回去。这条是「打错一个字不至于丢掉远程机器」的唯一保障。',
    inboundExposeWarn:
      '监听在 127.0.0.1 之外＝把代理暴露给整个网络。请先给某个账号设代理密码（账号页），否则网关会拒绝这次修改。',
    inboundSaved: '入站监听已生效——把客户端改指到 {{addr}}',
    inboundRevert: '{{sec}} 秒后入站监听将回滚到 {{addr}}',
    inboundKeep: '保留新地址',
    inboundConfirmed: '已保留新监听地址',

    final: 'Final 出口',
    finalHint: '已许可但没有任何路由规则认领的流量走哪。`proxy`＝经你的节点；`direct`＝从本机原样出网。',
    dns: 'DNS 解析策略',
    dnsHint: '服务器、分流规则与直连解析器。DNS 必须跟随路由，否则国内站点会解析到出口节点所在地区的 CDN 边缘。',
    proxyTuning: '节点切换与打分',
    proxyTuningHint: '代理组切换的积极程度，以及按真实流量（而非探测）给节点打分的权重。',

    autoBlock: '自动处置',
    autoBlockHint:
      '威胁情报命中、以及向**未许可**目的地的外泄形状上传，会被直接断连并隔离。启发式（beacon / DGA）永远只告警，绝不断连。',
    warmPermit: '等待许可索引就绪',
    warmPermitHint:
      '远端规则集是异步加载的。在它落地之前，它所许可的一切都读作「未许可」——此时处置会把你其实批准过的目的地封掉。告警照常，只有处置会等。',
    detThresholds: '连接侧阈值',
    detThresholdsHint: 'beacon / DGA / 外泄的阈值。这些以前全是编译进二进制的常量——一条要重新编译才能调的告警流，没人调得动。',
    detObservation: '查询与主机观测',
    detObservationHint: 'DNS 查询级检测、客户端 DoH/DoT 绕过、路由表监控与 JA4 指纹学习期。',

    retention: '日志与历史',
    retentionHint:
      '守护进程日志与连接历史各自允许占多少磁盘。这些以前只是启动参数，于是一次裸重装就会把它们静默重置。',
    retentionTitle: '日志与历史保留',
    retentionDesc: '<data>/serve.log 与 <data>/history.jsonl 的轮转策略。',
    retentionLog: '守护进程日志',
    retentionHistory: '连接历史',
    maxSize: '超过多少 MB 轮转',
    maxSizeHint: '当前文件到这个大小就换新文件。-1＝永不轮转（仅日志可用）。',
    maxBackups: '保留几代',
    maxBackupsHint: '超出这个数量的旧代会被删除。',
    maxAge: '超过多少天删除',
    maxAgeHint: '0＝只按个数保留，不按时间。',
    compress: 'gzip 压缩已轮转的文件',
    compressHint: '压缩是异步的，所以同一代会短暂地同时以 .jsonl 和 .jsonl.gz 存在。',
    historyNoRotateHint: '历史不允许设成永不轮转——一个无上限的追加文件迟早会写满磁盘。',
    retentionSaved: '保留策略已生效',

    openReg: '开放注册',
    openRegHint: '允许任何能打开控制台的人自行注册。默认关闭；新账号一律是 client，绝不会是 admin。',
    install: '安装信息',
    version: '版本',
    dataDir: '数据目录',
    privileged: '特权',

    language: '语言',
    languageHint: '控制台显示语言。默认跟随系统语言；在这里选过之后保存在本浏览器。',
    theme: '主题',
    themeHint: '不选就跟随系统设置。保存在本浏览器。',
    themeSystem: '跟随系统',
    themeDark: '深色',
    themeLight: '浅色',

    tun: {
      title: "TUN 高级选项",
      desc: "TUN 捕获模式（接管全设备流量的 VPN）的高级调优。仅在捕获模式为 TUN 时生效；Manual/System 下忽略。",
      stack: "网络栈",
      stackDesc: "gvisor=用户态，安全通用 · system=更快，需权限 · mixed。",
      mtu: "MTU",
      mtuPh: "0 = 自动",
      strict: "严格路由",
      strictDesc: "强制所有包走 TUN（推荐——防泄漏）。",
      redirect: "自动重定向（Linux）",
      redirectDesc: "用 nftables 把 Docker/containerd bridge 容器出网拉进同一套 Permit/检测。macOS/Windows 无效。需要 nft。",
      address: "TUN 地址",
      addressPh: "每行一个 CIDR — 留空 = 198.18.0.1/30（避开 Docker）",
      addressDesc: "TUN 设备接口地址。留空使用默认值，位于 Docker 常用的 172.16/12 之外。",
      redirectNftMissingTitle: "缺少 nftables（auto-redirect 依赖）",
      redirectNftMissingBody: "Linux TUN 的 auto-redirect 需要 nftables。{cmd}（需要 root/admin）。如果自动安装不可用，请手动安装 nftables。",
      redirectNftInstall: "一键安装 nftables",
      redirectNftInstalled: "nftables 安装完成 — 可在状态/日志里验证",
      exclude: "排除应用",
      excludePh: "每行一个——绕过 TUN 走直连",
      excludeDesc: "这些应用绕过 TUN 直连（Android 包名）。",
      include: "仅包含",
      includePh: "每行一个——只有这些进入 TUN",
      includeDesc: "若填写，则仅捕获这些应用（与“排除”互斥）。",
      save: "保存",
      toast: "TUN 选项已保存",
    },

    restoreDefaults: '恢复默认',
    save: '保存',
    cancel: '取消',
    defaultNote: '（默认 {{v}}）',
    usingDefault: '默认',
  },
};
