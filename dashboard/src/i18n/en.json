{
  "brand": {
    "subtitle": "gateway"
  },
  "nav": {
    "overview": "Overview",
    "connections": "Connections",
    "nodes": "Nodes",
    "endpoints": "Endpoints",
    "profiles": "Profiles",
    "whitelist": "Whitelist",
    "blacklist": "Blacklist",
    "ruleSets": "Rule Sets",
    "customRules": "Custom Routing",
    "proxies": "Proxies",
    "rules": "Rules",
    "dns": "DNS",
    "history": "History",
    "detection": "Detection",
    "logs": "Logs",
    "fleet": "Gateways",
    "settings": "Settings",
    "acls": "Policy",
    "vpn": "VPN",
    "grpMonitor": "Monitor",
    "grpPolicy": "Policy",
    "grpEgress": "Egress",
    "grpSystem": "System"
  },
  "top": {
    "capture": "Capture",
    "routing": "Routing",
    "posture": "Posture",
    "postureStrict": "Strict",
    "postureSplit": "Split",
    "postureStrictTip": "Default-deny: only permitted destinations can leave. Routing still picks the exit.",
    "postureSplitTip": "Default-allow: unmatched traffic uses Final (proxy by default). Route rules still pick exits; blacklist floor stays on.",
    "postureForcedRule": "Clash routing forced back to Rule — Global would bypass Split’s route rules.",
    "splitBanner": "Split: gate open — unmatched traffic → Final (proxy). Explicit routes still apply (direct / Overseas / …). Blacklist & threat intel still block.",
    "routingGlobalDisabledSplit": "Global is disabled in Split — it would ignore your route rules.",
    "autoBlock": "Auto-block",
    "autoBlockTip": "Auto-block drops threat-intel hits and large uploads (≥10 MiB) to destinations NOT on the Permit whitelist — then adds them to the Deny list so retries can't reconnect (including LAN/private). Heuristics (beaconing / DGA) only alert.",
    "live": "live",
    "up": "↑",
    "down": "↓",
    "total": "total",
    "keepMode": "Keep current mode",
    "guard": "Mode guard: reverting to {{to}} in {{sec}}s — confirm you still have access.",
    "captureMode": "Capture mode: {{mode}}",
    "tunNeedsRoot": "TUN needs elevated privileges (start with sudo)",
    "tunHelp": {
      "title": "TUN mode needs elevated privileges",
      "intro": "TUN captures ALL traffic at the network layer, so it must create a virtual interface — that needs root / admin. It's also mutually exclusive with other TUN tools (Surge enhanced mode, other VPNs).",
      "mac": "macOS: start the whole gateway with sudo — sudo ./trust-proxy serve -c configs/config.tun.json  (setcap isn't available on macOS)",
      "linuxSudo": "Linux: start with sudo,  or",
      "linuxSetcap": "Linux: grant the binary the capability once, then run as a normal user — sudo setcap cap_net_admin,cap_net_bind_service=+ep ./trust-proxy",
      "win": "Windows: run the app as Administrator.",
      "guard": "Safe to try: a TUN switch arms a 60s auto-revert, so if it severs your access the gateway falls back to the previous mode on its own. Switching back to Manual never needs privileges.",
      "cancel": "Cancel",
      "tryAnyway": "Try anyway"
    },
    "routingRuleTip": "Rule: whitelist default-deny (safe)",
    "routingGlobalTip": "Global: default-deny OFF — unlisted traffic egresses via proxy",
    "globalBanner": "Global routing: whitelist default-deny is OFF — unlisted traffic egresses via proxy (blacklist / threat-intel / process·device gates still apply). Switch back to Rule to restore default-deny.",
    "intel": "{{domains}}d / {{ips}}ip intel"
  },
  "settings": {
    "pageDesc": "Gateway configuration.",
    "language": "Language",
    "languageDesc": "Console display language. Defaults to your system language; a choice here is saved in this browser.",
    "inbound": {
      "title": "Proxy inbound auth",
      "desc": "Require a username + password on the local proxy inbound (SOCKS/HTTP at 127.0.0.1:21584). Leave both empty to keep it open (the default). Turn this on if other devices can reach the proxy port.",
      "user": "Username",
      "pass": "Password",
      "ph": "(empty = open)",
      "onHint": "Auth required for clients on :21584.",
      "offHint": "Inbound is open — no auth required.",
      "save": "Save",
      "toastOn": "Proxy inbound auth enabled",
      "toastOff": "Proxy inbound auth disabled (open)"
    },
    "tun": {
      "title": "TUN options",
      "desc": "Advanced tuning for TUN capture mode (full-device VPN that captures ALL traffic). Only applies when the capture mode is TUN; ignored in Manual/System.",
      "stack": "Network stack",
      "stackDesc": "gvisor = userspace, safe & portable · system = faster, needs privileges · mixed.",
      "mtu": "MTU",
      "mtuPh": "0 = auto",
      "strict": "Strict route",
      "strictDesc": "Force every packet through the TUN (recommended — prevents leaks).",
      "exclude": "Exclude apps",
      "excludePh": "one per line — routed AROUND the TUN",
      "excludeDesc": "These apps bypass the TUN and go direct (Android package names).",
      "include": "Include only",
      "includePh": "one per line — ONLY these go INTO the TUN",
      "includeDesc": "If set, ONLY these apps are captured (mutually exclusive with Exclude).",
      "save": "Save",
      "toast": "TUN options saved"
    }
  },
  "common": {
    "empty": "empty",
    "loading": "Loading…",
    "searchPlaceholder": "Search…",
    "prevPage": "Previous page",
    "nextPage": "Next page",
    "paginationRange": "{{from}}–{{to}} of {{total}}",
    "paginationPage": "{{page}} / {{pages}}"
  },
  "pages": {
    "acls": {
      "desc": "Strict policy: Permit (may leave?) is orthogonal to Route (which exit?). Route never opens the gate. Deny beats Permit. Subjects optionally restrict which processes/devices may egress.",
      "permit": "Permit",
      "route": "Route",
      "deny": "Deny",
      "subjects": "Subjects",
      "permitHint": "Destinations that may leave the network. Also granted by packs/rule-sets with a permit role — see Rules → Routing for the full list. Empty = deny all.",
      "subjectsHint": "Optional source gates. When non-empty, unlisted processes or devices cannot egress even to a permitted destination."
    },
    "whitelist": {
      "desc": "Permit destinations (domains & IPs). Default-deny: anything not listed (and not granted by a permit pack/rule-set) is blocked. Processes & Devices are Subjects, not destinations.",
      "domains": "Domains",
      "domainsHint": "Suffix by default (example.com covers its subdomains). Wildcards: *.example.com (subdomains), foo* (prefix).",
      "domainsPh": "example.com  /  *.example.com",
      "ip": "IP / CIDR",
      "ipPh": "1.2.3.4/32",
      "processes": "Processes",
      "processesHint": "When non-empty, unknown binaries can't egress at all — even to a permitted destination.",
      "processesPh": "curl  /usr/bin/ssh",
      "devices": "Devices (source)",
      "devicesHint": "When non-empty, only these source IPs/CIDRs may egress. For gateway/router deployments.",
      "devicesPh": "192.168.1.20"
    },
    "blacklist": {
      "desc": "Hard deny. These destinations are REJECTED even if Permit would otherwise allow them — deny sits above the Permit gate.",
      "domains": "Domains",
      "domainsHint": "Suffix by default (evil.com blocks its subdomains). Wildcards: *.evil.com, ads-* (prefix).",
      "domainsPh": "evil.com  /  *.evil.com",
      "keywords": "Keywords",
      "keywordsHint": "Matched as a substring anywhere in the domain.",
      "keywordsPh": "tracker",
      "regexes": "Regexes",
      "regexesHint": "RE2 pattern matched against the domain (must compile).",
      "regexesPh": ".*\\.onion$",
      "ip": "IP / CIDR",
      "ipHint": "Reject connections to these destination IPs/CIDRs.",
      "ipPh": "6.6.6.6/32"
    },
    "directlist": {
      "desc": "Route only: permitted traffic to these destinations egresses DIRECT. This never grants Permit — add the destination under Permit (or enable a permit pack) first. Private/LAN ranges are always routed direct automatically.",
      "domains": "Domains",
      "domainsHint": "Suffix by default (corp.local covers its subdomains). Wildcards: *.corp.local, intra-* (prefix).",
      "domainsPh": "corp.local  /  *.corp.local",
      "ip": "IP / CIDR",
      "ipHint": "These destination IPs/CIDRs egress direct (never through the proxy) — only if already permitted.",
      "ipPh": "203.0.113.0/24",
      "builtinTitle": "Built-in (LAN / private)",
      "builtinHint": "Common private, loopback, link-local and CGNAT ranges are always routed direct when traffic is permitted — no need to add them."
    }
  }
}
